# Stage 5D-3 browser evidence

Captured once from the production build at
`http://127.0.0.1:4173/probpera-literary-map/#books` on 2026-08-27.

- Desktop `1440×900`, idle: one Canvas, zero document/frame overflow,
  `SHELF_IDLE`.
- Desktop `1440×900`, selected: exact allowed cover, one Canvas, detail panel,
  `INSPECTION_CLOSED`.
- Mobile `390×844`, idle: bounded nine-book pool, one Canvas, zero overflow,
  `SHELF_IDLE`.

Raw geometry, readiness times and SHA-256 values are stored in
`qa-results.json`.
