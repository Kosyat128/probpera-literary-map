# Evidence V2 registry transition, 2026-09-12

Approved scope: seven additive source authorities; the validator and all historical migration/checksum pins stay unchanged. Canonical UTF-8 registry identity moves from `d0428d265845b68d6d5ee2ad9828353c91456eb5e57baf0f639702b8656044ef` to `c8d2b6862c47c3215295951d2c5d1c406913b9879c616f1d8b787c6e05029f6c`. The old Windows file-byte hash is not the database identity.

The existing service-role atomic archive commit is the only publication path. A forward migration changes the default for new controls and installs an exact transition capability; it does not rotate the live singleton. The unchanged guarded reconciliation workflow first backs up production, restores and reconciles an isolated copy, verifies the plan and main revision, and then invokes the existing archive release.

The commit retains its lock order, staged manifests, live snapshot checks, CMS preservation, idempotent receipt, and enforcement checks. At the rotation boundary it requires the exact old/new identities, every prior-public key, fresh staged evidence for every prior-public unlocked work, and checksum-bound attestation-only proofs rebuilt from the exact live CMS content. Only then can the same transaction switch the active registry and re-attest those CMS works. All retained predecessors must remain public and attested before commit. Any failure rolls back the registry, data, and proofs together; enforcement is never disabled.

The private snapshot includes `contentText`, the exact PostgreSQL `content::text` representation. JavaScript verifies its UTF-8 SHA-256 and equality of its parsed value to `content`; it does not reconstruct PostgreSQL JSONB formatting. The coverage envelope similarly carries `coverageText`: SQL verifies both its exact text hash and equality of its parsed JSONB value to the supplied predecessor keys and CMS proofs. After the unchanged live hash/content/timestamp guards run under the existing table locks, re-attestation receives the live JSONB value so numeric scale such as `1.00` survives the published attester's own text-hash check.

CMS content, locks, original review dates, and review identities are preserved by registry rotation. Local overlays cannot repair or substitute a CMS snapshot. An unknown registry, changed snapshot, missing old-public work, invalid current proof, or a failed unchanged validator stops publication. Wells has a separate exact editorial repair packet in `reports/wells-editorial-repair-20260913.json`: independently AI-reviewed new RU/EN synopses, the supplied title correction, originals archived in metadata, and a fresh proof under the unchanged old registry. Its service-only RPC runs before archive preflight and preserves the original work description, CMS lock, and artworks. This editorial transaction is separate from the full archive commit; if later archive preflight fails, its successful repair remains recorded by its own receipt. No historical authorship or human review is inferred. Production readiness still requires successful execution of that guard against the reviewed snapshot.

Operational rollback is the existing verified encrypted backup/restore procedure. There is no separate registry downgrade RPC or unguarded production SQL step. A failed atomic commit needs no restore: its previous identity and attestations remain intact. A committed retry returns the existing receipt without rotating twice.

Two new archive-author references are explicitly authorized. Stowe (`usa:harriet_beecher_stowe`, Гарриет Бичер-Стоу / Harriet Beecher Stowe) requires the exact fresh staged proof for `uncle-toms-cabin`. Alcott (`usa:louisa_may_alcott`, Луиза Мэй Олкотт / Louisa May Alcott) is separately limited to the unverified `little-women` first part, 1868, with draft status and no attestation. Its source is MASTER `/records/9645`, canonical source-record SHA-256 `b21364f9bb413707c39e6023ebec3de5fa0b1da41e38f2f670cef3795badd231` (MASTER file SHA-256 `f441344f6b4367684c8f5ec39f746f12c079ff05e5130b3183e06bab7c359674`). Both references carry `stagedItemSha256`, the SHA-256 of the entire item from the unchanged canonical staging transport. SQL compares the stored `payload_sha256` only after the outer commit has verified its exact `canonical_payload` bytes and parsed payload. Missing writer references are inserted inside the existing transaction before the nondeferrable work FK is used. Existing matching manual references remain untouched, conflicts block, and a NOWAIT reference-table lock prevents waiting against an interactive edit. No biography or public verification is created by these reference hooks.

Validation: the exact forward SQL migration executes twice in a local isolated PGlite 0.5.8 engine (219 fixture statements), with actual FK checks, stale identity/content/date/coverage failures, missing prior evidence, late-error rollback, preservation of manual references, draft-versus-attested behavior, enforcement preservation/enablement, and committed retry. The serialization regressions reject missing, malformed, changed or semantically different coverage text and substituted staged content; valid alternate JSON formatting and CMS numeric scale remain accepted without changing stored content. A separate JavaScript/PostgreSQL transport fixture checks the generated snapshot, both writer references, and coverage envelope against actual PostgreSQL hashes. PostgreSQL Docker execution of both fixtures is mandatory in CI; absence of local Docker is reported as skipped, not passed. Separate local tests use the real unchanged TypeScript Evidence V2 validator against a full live-shaped CMS projection and verify that missing private provenance cannot be replaced by a local reviewed card. No production SQL or publication was performed for this implementation.

Unpublished review lineage: the earlier rotation migration SHA-256 `01a7fc62c731d01c2ea6912dc4ed770bf5cbe4d696122feb4fee401987ff4220` and its 177-statement receipt are retained in the common package report as a superseded implementation review. The remote checkpoint tag `codex/books-r49n-checkpoint-20260912-459f2d70` and all 35 published migration pins remain unchanged. This correction changes only the unpublished forward contract and its current release plan.
