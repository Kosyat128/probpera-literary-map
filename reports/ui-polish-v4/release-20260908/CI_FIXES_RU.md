# Уточнение браузерной приёмки перед публикацией

2026-09-08. Пользователь разрешил публикацию. PR: https://github.com/Kosyat128/probpera-literary-map/pull/186. Слияние и публикация на момент этой записи ещё не выполнены.

Проверка Quality and security 34206999527 на `68d4da8905a23fa72b2fdc205d82b4124949232e` прошла типы, unit, контент, ссылки, медиа, сборку, SEO и performance budget. Она остановилась на desktop-сценарии `edition rail moves focus without changing the rendered edition`; последующие браузерные этапы не запускались. Это не успешный полный прогон.

Трассировки обеих попыток подтвердили: загрузка глобуса завершалась примерно за 8,5 с, клавиатурные проверки выбора, скрытия, inert и раскрытия панели проходили до 33 с. Затем обычный клик перехватывали закреплённая шапка и canvas при автопрокрутке; повторы занимали 12,7-15,4 с до teardown на 45-й секунде. Ошибки hidden/null возникали при завершении теста. Тайм-аут не увеличен, force-click не применяется.

Изменён только `tests/e2e/premium-globe-exploration.spec.mjs`:

- Перед pointer-click лента размещается ниже обеих закреплённых строк навигации. Проверяются видимость и попадание в кнопку через `elementFromPoint`, затем выполняется обычный клик. Допуск intersection ratio 0,99 учитывает подтверждённое округление края горизонтальной ленты: desktop 0,99255, mobile 0,99858.
- Выравнивание кнопок проверяется по видимому содержимому: подписи на desktop и иконке на mobile. Сохранены проверки отсутствия обрезки и точности центра 0,5 px; добавлены доступные названия и минимальная область 44 × 44 px.
- Проверка выбора страны учитывает согласованное увеличение мобильной сцены 880 → 1000 px. Вместо требования одинаковой высоты она проверяет заполнение реальной сцены, неуменьшение высоты и сохранение исходного Canvas. Desktop equality сохранена.

Локальные результаты: desktop/mobile выбора страны прошли за 6,7/10,5 с; финальные desktop/mobile выбора издания - за 8,2/8,8 с. Всего четыре затронутых сценария прошли. До исправления mobile-проверки воспроизводимо падали на скрытой подписи и изменении высоты на 120 px. Первый вариант дополнительной viewport-проверки также зафиксировал округление рамки; это уточнение теста, не изменение продукта.

Код приложения, стили и каталоги после `68d4da89` не менялись. Повторная обязательная проверка GitHub остаётся необходимой; локальные результаты не заменяют её. Историческая приёмка main-integration сохраняет собственную дату и область проверки.

## Дополнение после повторного CI

Run 34209192524 остановился на двух длинных тире в этом новом отчёте. Пунктуация исправлена каноническим normalizeShortHyphens; изменённые файлы прошли его проверку.

Run 34209552284 на `6ea4b016cd49ec78f88cc6ba8d83ba0cc8b3dc43` повторно прошёл типы, unit, контент, медиа, сборку, SEO и performance. В desktop-тесте выбора изданий перехватов клика больше нет: hit-test, обычный клик, скрытие и финальное раскрытие панели успешны в trace. Однако программный GPU при frame-mode=always замедлял каждый шаг; последние assertions завершались на 45,926/46,536 с, после общего лимита 45 с. Этот запуск остаётся неуспешным.

В setup только этого сценария непрерывное вращение теперь останавливается штатной кнопкой, с обычным кликом и проверками aria-pressed=false и frame-mode=demand. Проверки клавиатуры, выбора, inert, восстановления фокуса, клика и hover сохранены; тайм-аут 45 с не менялся. Автовращение проверяется отдельным существующим сценарием.

Локально прошли все четыре desktop/mobile сценария выбора изданий и жизненного цикла автовращения (7,9/8,9/8,3/9,1 с). Дополнительно точный desktop-сценарий прошёл в Chrome с явными launch flags программного SwiftShader за 19,6 с. Это отдельный локальный прогон, не объявляемый результатом Linux CI. Код продукта по-прежнему неизменён; публикация зависит от нового успешного verify.

## Разделение длинной проверки интерфейса

Run 34211243880 на `cfec0c20a4e0695fe79a6c60e7e38404f29893e3` подтвердил исправление выбора изданий: shard 2 прошёл 2/2. Следующий прежний составной сценарий геометрии, подборки и архивов исчерпал 45 с до начала immersive-проверок. Trace обеих попыток показывает успешные предшествующие assertions без перехватов кликов; enter-immersive вызывался уже после начала teardown. Все этапы до браузерных тестов вновь прошли.

Остановка вращения в setup этого составного сценария прошла локально на desktop/mobile (12,0/10,0 с), но software GPU воспроизвёл накопленный timeout. Промежуточное разделение на две части также было недостаточным для длинной последовательности архивов. Эти диагностические неуспешные запуски не объявляются пройденными.

Итоговая проверка разделена на три самостоятельных контракта: геометрия и взаимоисключение панелей; количество стран в rich-подборке и встроенные архивы; полноэкранные фильтры и порядок Escape. Исходные assertions, допуски и лимит 45 с сохранены, в том числе все 12 исходных immersive assertions. Каждая часть создаёт своё нужное состояние реальными действиями. Setup останавливает вращение через focus + Space штатной кнопки, проверяя фокус, aria-pressed=false и demand; отдельный существующий тест активации мышью не менялся.

Финальные шесть сценариев прошли с явным software GPU, отдельным процессом Chrome на каждую пару desktop/mobile: геометрия 35,6/26,8 с; подборка и архивы 34,1/43,7 с; immersive 34,8/36,5 с. Это локальные результаты; новый GitHub verify остаётся обязательным. Дополнительно неизменённые random/coarse/Nobel/writer/manual-drag/rapid-selection прошли в обычном Chrome 12/12, а визуальные эталоны immersive chrome и карточек писателей 2/2 без обновления PNG.

## Мобильная сводка Nobel и доступность кнопок под закреплённой шапкой

Run 34214351776 на `10b6f5d87e507ed18e8c07b47cb6ee66c1ee76f3` прошёл первые 11 premium-сценариев, включая все три разделённых контракта. Затем Nobel desktop оказался flaky (retry прошёл), а writer desktop завершился по общему timeout 45 с. Trace подтвердил длительные повторы обычного открытия поиска/фильтров под закреплённой шапкой: 27,3 с у Nobel и 35,4 с у writer. В повторе writer промежуточные проверки страны, маркера, intent и Canvas прошли; последний поиск снова попал под шапку. Этот CI остаётся неуспешным.

Общий helper для восьми вызовов discovery-кнопок теперь размещает реальную кнопку ниже закреплённой навигации, проверяет попадание в её центр и выполняет обычный click. В immersive дополнительная прокрутка не применяется. Force-click, DOM-click и подмена событий не используются; исходные assertions сохранены.

Отдельная локальная проверка с software GPU обнаружила реальный дефект продукта: мобильная кнопка управления перекрывала сводку Nobel. Исправлен только mobile embedded CSS: сводка начинается на 208 px, ниже 44 px кнопки с зазором 16 px. Добавлен регрессионный guard: активный глобус прокручивается в поле зрения, затем проверяются видимость кнопки и реальный зазор не менее 8 px перед обычным кликом по сводке. Полноэкранное оформление не менялось.

Новый fingerprint продукта: `39324a8c1698df015cb3beaf8244d727d803aaa4ed65242e00610cfb9794ca9e`. Все 10 шагов обновлённой сборки, CSS-аудит, domain/SEO/performance и доставка dossier прошли; source после сборки не менялся. `nobel-fix/build-result.json` содержит команды и exit codes. Снимки и геометрия в `nobel-fix/visual/`: на 320/390 px зазор ровно 16 px, обычное раскрытие работает, overflow и page errors отсутствуют. На 768 px проверены отсутствие перекрытия и раскрытие сводки; это не заявление о размещении всей высокой сцены в одном viewport.

Два длинных мобильных lifecycle-сценария (coarse touch ownership и rapid A/B/Iceland с resize) сохраняют полную последовательность в одной сессии и получают общий бюджет 90 с. Исходные 45 с прогоны завершались до конца последовательности; отдельные diagnostics с 90 с прошли за 65,605/67,437 с без изменения assertions. Их результаты учитываются отдельно от исходных failures. Desktop и assertion timeouts не увеличены. Общий лимит влияет также на ожидание действий; независимого actionTimeout в конфигурации нет.

Финальная локальная проверка нового продукта: Nobel desktop/mobile в обычном Chrome 2/2; связанные панели и архивы 6/6; Nobel и writer с явным SwiftShader 4/4, без retries и skips. Сводка `nobel-fix/targeted-results.json` сохраняет время, проект и результат каждой проверки. Software длительности Nobel 23,0/31,6 с, writer 27,7/47,1 с (включая hooks). Кадр `visual/before-overlap-mobile.jpeg` взят из исходной неуспешной трассировки; три `nobel-controls-*.png` сняты после исправления. Неизменённые каталоги повторно не синхронизировались. Эти локальные результаты не заменяют следующий обязательный GitHub verify и последующую проверку опубликованного commit SHA.

## Полный жизненный цикл маркера писателя

Run 34217635165 на `134bec893750ff5be404d509b4d9f1944c48e165` прошёл все предшествующие build/audit этапы и 12 premium-сценариев, включая Nobel. Остановился только на writer desktop, две попытки по 45 с. Выбор писателя, явное отображение маркера и сохранение Canvas прошли к 29,308/30,346 с. Закрытие панели завершилось к 31,763/32,751 с. После этого frame-mode=always замедлял каждое позднее locator.evaluate до 5,05-5,46 с: hit guard второго поиска начался на 43,868/44,861 с, а общий teardown на 44,761/44,919 с. Единственный callback guard завершился уже после deadline; его boolean в trace не записан. До обычного discovery click выполнение не дошло. Два перехвата шапкой относятся к закрытию панели и успешно разрешились; нового дефекта второго search target трассировка не доказывает.

Полная последовательность Japan -> выбор писателя -> явный marker -> закрытие -> Andorra -> отсутствие marker остаётся в одной сессии и на том же Canvas. Простое выделение последней части в новый тест теряло бы проверку очистки реально установленного маркера. Поэтому только этот сценарий получает общий бюджет 90 с на обоих проектах, с дополнительными sheet-действиями на mobile. Все assertions, hit guard, обычные клики и лимиты 5/10 с сохранены. Приложение и fingerprint `39324a8c1698df015cb3beaf8244d727d803aaa4ed65242e00610cfb9794ca9e` не менялись; повторная сборка не требуется.

Отдельная desktop software диагностика с общим бюджетом 90 с прошла за 30,0 с. Однако следующий mobile прогон воспроизвёл именно false hit guard с собственным timeout 10 с на втором поиске; этот failure не скрыт увеличенным общим бюджетом. Helper уточнён: измерение, прокрутка и hit-check выполняются в одном browser evaluate и повторяются вместе, пока цель не доступна. Прежний вариант прокручивал один раз, затем повторял только hit-check по положению, которое могло измениться при закрытии sheet. Это также устраняет два отдельных дорогих browser round trips. Обычный click остаётся после успешного guard; timeout guard не изменён.

Локальная трассировка сохранила 12 завершённых false callbacks за 10 с: после одноразового alignment scrollTop оставался 1204, discovery-кнопки были частично за закреплённой навигацией. Точный источник сдвига страницы trace не записал; причинность конкретного focus callback не утверждается. После повторяемого alignment+hit-check полный software writer desktop/mobile прошёл 2/2 за 27,2/36,7 с. Три затронутых контракта панелей в обычном Chrome прошли 6/6. Immersive сохраняет обычный click без дополнительного выравнивания и hit guard. `nobel-fix/writer-lifecycle-results.json` разделяет diagnostics, исходный mobile failure и финальные восемь успешных проверок; retries/skips в финале отсутствуют. Следующий GitHub verify остаётся обязательным.

## Общая палитра и оставшиеся регрессии WebGL

Run 34220034247 на `fd51b33f4ad6d052755cc905f7ea883b1fd2f0e9` полностью прошёл premium: 28 passed, 0 failed/flaky/skipped во всех 22 shard. WebGL shard 1/6 завершился с 30 passed, 1 failed, 1 flaky, 1 skipped. Остальные пять shard, общий browser regression и финальный dependency audit не запускались. `CI_RUN_34220034247_COUNTS.json` сохраняет отдельные категории; весь run остаётся failed.

NASA surface test ожидал старую панель `rgba(30, 9, 45, 0.9)`, тогда как согласованный общий CSS controls задаёт `rgba(27, 9, 38, 0.96)`. Исправлено только ожидаемое значение цвета. Проверки shared-starry, фона, vignette, switcher и переключения трёх изданий сохранены. Software desktop/mobile прошли 2/2 за 30,6/28,6 с.

Flaky filters test использовал оставшийся старый helper с прямым discovery click. Первый клик занял 52,219 с из-за повторяющихся перехватов шапкой/canvas. Последующие all/nobel/rich/portrait прошли; verified завершился уже после общего 90 с deadline. Atomic alignment+hit-check с обычным click устранил подтверждённую причину; все пять коллекций, идентичность Canvas и прежний лимит 90 с сохранены. Software desktop/mobile прошли 2/2, без retries/skips. Код продукта и fingerprint `39324a8c1698df015cb3beaf8244d727d803aaa4ed65242e00610cfb9794ca9e` не менялись.

Тот же подтверждённый класс прямых embedded discovery кликов найден ещё в восьми местах трёх WebGL specs: controls, responsive-reader и public-smoke. Они используют общий `helpers/atlas-discovery.mjs`, скопированный из уже прошедшего premium helper. Runtime делегирует ему открытие панели. Условное открытие, переключения, Escape, фокус, все 298 testcase assertions четырёх затронутых specs на этом шаге и существующие тайм-ауты сохранены; отдельное изменение NASA literal описано выше. Premium spec полностью неизменён. Immersive-only controls в эту замену не включены.

Локальная оставшаяся серия в обычном Chrome выполнила WebGL shard 2-6: 137 passed, 3 failed, 21 предусмотренный skip. Отдельный regression lane прошёл 37/37 выполненных сценариев, 3 предусмотренных skip. Совокупно: 174 passed, 3 failed, 24 skipped, без retries/flaky. Эти исходные результаты сохранены, а не переименованы в зелёную приёмку. Продукт и сборка оставались неизменны; формы не отправлялись в production.

Три падения относятся к двум подтверждённым предпосылкам тестов:

- Immersive-геометрия на 820 px измерялась в начале существующей конечной CSS-анимации `atlas-premium-controls-arrive`, когда панель имела translate 9 px. Кадр после анимации показывал выравнивание. Теперь тест дожидается завершения именно этой анимации перед единым снимком геометрии; допуск 2 px и вся матрица 320/820/1366 сохранены. Точный desktop-сценарий прошёл с software GPU за 20,6 с.
- Desktop/mobile smoke чтения искал заголовок текстового режима внутри намеренно скрытого контейнера при открытом иллюстрированном режиме. Теперь перед этой проверкой выбирается штатная кнопка «Светлый режим». Рекомендации проверяются по действующим видимым ссылкам, с новым требованием непустого списка и прежней проверкой уникальности; проверка горизонтального разрыва сохранена. Software desktop/mobile прошли за 5,5/16,5 с.

`runtime-fix/closure-results.json` сохраняет 201 исходный normal-case и семь целевых software-проверок: поверхности 2/2, фильтры 2/2, геометрия 1/1, чтение 2/2. Все семь прошли без retries/skips. Это цепочка закрытия конкретных расхождений, а не новый полный CI. Следующий обязательный GitHub verify на окончательном PR head остаётся условием слияния.

## Ограничение пути служебной съёмки

На `87f2956476ab17faa13a84fa2e8dfafe5b111a8d` анализаторы CodeQL завершили свои jobs успешно, но сводный security check `102065664206` сообщил два high alerts `Uncontrolled data used in path expression` в `scripts/capture-ui-polish-v4.mjs`. Успех jobs анализа не означал отсутствие замечаний. Общий verify на момент исправления продолжал browser-проверки; его результат не объявляется успешным.

CLI-аргумент phase прежде включался в путь без проверки. Теперь допустимо только имя из 1-64 строчных латинских букв, цифр и дефисов, начинающееся с буквы. Дополнительно разрешённый абсолютный путь проверяется через `path.relative` как непустой потомок `reports/ui-polish-v4`. Корень репозитория определяется из расположения скрипта, а не текущего рабочего каталога. Проверка выполняется до создания папки и запуска браузера. Имена before/after/working и прочие допустимые leaf names сохранены; подавлений CodeQL нет.

75 проверок путей Windows/POSIX, node syntax и diff check прошли. Проверки не запускали браузер; продукт и его сборка не менялись. Фактическое закрытие двух alerts требует нового анализа CodeQL, как и успешный required verify на окончательном head.

## Общий бюджет полного CI

Run 34227571378 на `87f29564` перед автоматической отменой новым push успел пройти все 22 premium shard: 28 passed, 0 failed/flaky/skipped; WebGL и общий regression в этом запуске не начались. Это частичный результат, не полный verify.

В ранее завершённом run 34220034247 путь до конца первого WebGL shard занял 36 мин 29,47 с: premium 11 мин 47,29 с, WebGL shard 1 - 18 мин 47,66 с. Оставшиеся пять shard даже в локальном обычном Chrome потребовали 17 мин 26,05 с; их длительность с программным GPU CI заранее неизвестна. Сумма уже составляет 53 мин 55,52 с до оставшегося regression, dependency audit и накладных расходов. Само превышение 60 минут ещё не наблюдалось; это расчёт риска исчерпания бюджета, а не новый failure.

Общий `verify.timeout-minutes` увеличен с 60 до 120, чтобы дать полному последовательному набору завершиться на программном GPU. Required check, все шаги, порядок shard, assertions, отдельные test/assertion timeouts и загрузка диагностик сохранены. Это единственная правка workflow; обходов проверок и новых skips нет. Изменение продукта не требуется.

## Языковые контракты иллюстрированной книги и исходное состояние портрета

Run 34229916452 на `ddc59fc26c0162342ae1124d7e397609b0df20b6` подтвердил закрытие CodeQL: итоговый summary SUCCESS, 0 новых alerts. Premium вновь прошёл 28/28 без flaky/skips. Первый WebGL shard: 30 passed, 1 failed, 1 flaky, 1 skipped; остальные shard и regression не запускались. Это failed run, не зелёная приёмка.

Длинный illustrated-book test выполнял полные RU и EN контракты последовательно в одном 180-секундном бюджете. Trace первой попытки: EN начался на 94,324 с, финальный Next успешно кликнул на 174,599-175,645 с, все четыре финальных page assertions завершились к 184,462 с; teardown начался на 180,012 с. Во второй попытке последний Next вызывался после 183,396 с, уже при закрытии контекста. Перехватов указателя и признаков блокирующего overlay не было. `reader-locale-fix/ci-diagnostic.json` сохраняет обе попытки.

Теперь каждый язык регистрирует самостоятельный полный test с прежними 180 с. Тело проверки всех страниц, текста, иллюстраций, навигации, геометрии, закладок и смены motion preference сохранено побайтно; setup повторяется в отдельном контексте каждого языка. Software desktop RU/EN и mobile RU/EN прошли 4/4 без retries/skips. Изменений ридера или продукта нет.

Flaky portrait baseline был снят после того, как прокрутка перенесла изображение под оставшийся от кнопки «Каталог» указатель. Trace подтверждает попадание указателя внутрь портрета; действующий hover CSS увеличивает только изображение до 1,025. Видимость также была подтверждена до загрузки изображения. Теперь setup выводит указатель за пределы карточки, проверяет отсутствие hover и готовность изображения; прежний object-fit cover и допуск 1 px проверяются через poll с действующим 10-секундным бюджетом. Остальные проверки сетки и обложек сохранены. Численная величина исходного transform в trace не записана и не выдумывается.

Точный desktop archive/portrait software test прошёл за 10,145 с без retry. `reader-locale-fix/targeted-results.json` объединяет пять финальных целевых успехов: desktop RU/EN, mobile RU/EN, desktop portrait; failed/flaky/skipped отсутствуют. Это локальные закрытия двух причин. Новый полный CI остаётся необходимым. Ранее недостигнутые оставшиеся WebGL shard будут дополнительно проверены с локальным software GPU параллельно серверному CI, поскольку обычный GPU не воспроизвёл подтверждённый накопленный timeout.

## Запас места в шапке у компактной границы

Run 34235910726 на `6383c26566ec9f179ec62f199dde96b772437cce` прошёл CodeQL и premium. WebGL shard 1: 31 passed, 1 flaky, 1 skipped; повторная попытка сценария поверхностей прошла после исходного общего timeout 120 с. Shard 2: 31 passed, 1 failed, 1 skipped. В обеих попытках исходного теста шапки на ширине 1521 px правая граница кнопок была 1524 px: подтверждён выход на 3 px. Остальные shard и regression в этом CI не достигнуты; автоматическое слияние остановилось до изменения main.

Существующая компактная шапка теперь сохраняется до 1600 px; полный набор возвращается на 1601 px. Шрифты, подписи, логотип, кнопки и исходные assertions не менялись. Локальная Windows-проверка не воспроизвела ровно исходные 3 px; ширина запасного шрифта остаётся подтверждённым риском, а не доказанной единственной причиной. На новой собранной версии исходный desktop test прошёл с software GPU: 1 passed, без retries/skips. Фактические кадры 1521/1601 px просмотрены; кнопки внутри viewport и не перекрывают навигацию. Геометрия, диагностика и кадры сохранены в `header-breakpoint-fix/`.

Все 10 шагов новой domain/preview сборки и сопутствующих проверок прошли: `header-fit-fix/build-result.json`. Список файлов для fingerprint расширен: прежний список UI delta не включал `src/index.css`; теперь включены CSS шапки и ранее изменённый capture script. SHA-256 текущего явно перечисленного набора файлов: `8e9a15098a6e2095f2f7103f1a1eb80206f2b64f74cfebdf128f74afc599678d`. Это не hash всего репозитория. Исторические fingerprints оставлены при своих датированных доказательствах.

Дополнительный локальный software GPU прогон оставшихся shard остановлен по просьбе пользователя экономить время и лимит: shard 2 и 3 прошли суммарно 64 passed, 2 skipped; shard 4 прерван, shard 5/6 не запущены. Эти результаты не заменяют полный обязательный CI на новом head. Слияние и публикация разрешены пользователем; проверка фактической production версии выполняется после успешного защищённого слияния.

## Согласование защиты шапки с подтверждённым исправлением

Run 34243131729 на `32d177f998a74d5f528e3c9d88a983daf0638541` остановился на unit tests: 3208 passed, 2 failed. Обе проверки закрепляли прежнюю границу шапки: точный regex 1520 px и исторический Header/Hero CSS fingerprint. Это не новый визуальный дефект; browser suite не запускался. Presentation contract теперь требует 1600 px. Для исторического CSS lock добавлена ограниченная проекция единственного целиком заданного compact блока обратно к 1520 px; старые manifests и immutable hashes сохранены. Отсутствующий, повторный или изменённый фрагмент отклоняется. Приложение после принятой сборки шапки не менялось; повторная сборка не требуется. Финальные точечные результаты и актуальный перечень hash сохранены в `header-fit-fix/lock-closure.json`.
