# Уточнение браузерной приёмки перед публикацией

2026-09-08. Пользователь разрешил публикацию. PR: https://github.com/Kosyat128/probpera-literary-map/pull/186. Слияние и публикация на момент этой записи ещё не выполнены.

Проверка Quality and security 34206999527 на `68d4da8905a23fa72b2fdc205d82b4124949232e` прошла типы, unit, контент, ссылки, медиа, сборку, SEO и performance budget. Она остановилась на desktop-сценарии `edition rail moves focus without changing the rendered edition`; последующие браузерные этапы не запускались. Это не успешный полный прогон.

Трассировки обеих попыток подтвердили: загрузка глобуса завершалась примерно за 8,5 с, клавиатурные проверки выбора, скрытия, inert и раскрытия панели проходили до 33 с. Затем обычный клик перехватывали закреплённая шапка и canvas при автопрокрутке; повторы занимали 12,7-15,4 с до teardown на 45-й секунде. Ошибки hidden/null возникали при завершении теста. Тайм-аут не увеличен, force-click не применяется.

Изменён только `tests/e2e/premium-globe-exploration.spec.mjs`:

- Перед pointer-click лента размещается ниже обеих закреплённых строк навигации. Проверяются видимость и попадание в кнопку через `elementFromPoint`, затем выполняется обычный клик. Допуск intersection ratio 0,99 учитывает подтверждённое округление края горизонтальной ленты: desktop 0,99255, mobile 0,99858.
- Выравнивание кнопок проверяется по видимому содержимому: подписи на desktop и иконке на mobile. Сохранены проверки отсутствия обрезки и точности центра 0,5 px; добавлены доступные названия и минимальная область 44 × 44 px.
- Проверка выбора страны учитывает согласованное увеличение мобильной сцены 880 → 1000 px. Вместо требования одинаковой высоты она проверяет заполнение реальной сцены, неуменьшение высоты и сохранение исходного Canvas. Desktop equality сохранена.

Локальные результаты: desktop/mobile выбора страны прошли за 6,7/10,5 с; финальные desktop/mobile выбора издания - за 8,2/8,8 с. Всего четыре затронутых сценария прошли. До исправления mobile-проверки воспроизводимо падали на скрытой подписи и изменении высоты на 120 px. Первый вариант дополнительной viewport-проверки также зафиксировал округление рамки; это уточнение теста, не изменение продукта.

Код приложения, стили и каталоги после `68d4da89` не менялись. Повторная обязательная проверка GitHub остаётся необходимой; локальные результаты не заменяют её. Историческая приёмка main-integration сохраняет собственную дату и область проверки.

## Дополнение после повторного CI

Run 34209192524 остановился на двух длинных тире в этом новом отчёте. Пунктуация исправлена каноническим normalizeShortHyphens; изменённые файлы прошли его проверку.

Run 34209552284 на `6ea4b016cd49ec78f88cc6ba8d83ba0cc8b3dc43` повторно прошёл типы, unit, контент, медиа, сборку, SEO и performance. В desktop-тесте выбора изданий перехватов клика больше нет: hit-test, обычный клик, скрытие и финальное раскрытие панели успешны в trace. Однако программный GPU при frame-mode=always замедлял каждый шаг; последние assertions завершались на 45,926/46,536 с, после общего лимита 45 с. Этот запуск остаётся неуспешным.

В setup только этого сценария непрерывное вращение теперь останавливается штатной кнопкой, с обычным кликом и проверками aria-pressed=false и frame-mode=demand. Проверки клавиатуры, выбора, inert, восстановления фокуса, клика и hover сохранены; тайм-аут 45 с не менялся. Автовращение проверяется отдельным существующим сценарием.

Локально прошли все четыре desktop/mobile сценария выбора изданий и жизненного цикла автовращения (7,9/8,9/8,3/9,1 с). Дополнительно точный desktop-сценарий прошёл в Chrome с явными launch flags программного SwiftShader за 19,6 с. Это отдельный локальный прогон, не объявляемый результатом Linux CI. Код продукта по-прежнему неизменён; публикация зависит от нового успешного verify.

## Разделение длинной проверки интерфейса

Run 34211243880 на `cfec0c20a4e0695fe79a6c60e7e38404f29893e3` подтвердил исправление выбора изданий: shard 2 прошёл 2/2. Следующий прежний составной сценарий геометрии, подборки и архивов исчерпал 45 с до начала immersive-проверок. Trace обеих попыток показывает успешные предшествующие assertions без перехватов кликов; enter-immersive вызывался уже после начала teardown. Все этапы до браузерных тестов вновь прошли.

Остановка вращения в setup этого составного сценария прошла локально на desktop/mobile (12,0/10,0 с), но software GPU воспроизвёл накопленный timeout. Промежуточное разделение на две части также было недостаточным для длинной последовательности архивов. Эти диагностические неуспешные запуски не объявляются пройденными.

Итоговая проверка разделена на три самостоятельных контракта: геометрия и взаимоисключение панелей; количество стран в rich-подборке и встроенные архивы; полноэкранные фильтры и порядок Escape. Исходные assertions, допуски и лимит 45 с сохранены, в том числе все 12 исходных immersive assertions. Каждая часть создаёт своё нужное состояние реальными действиями. Setup останавливает вращение через focus + Space штатной кнопки, проверяя фокус, aria-pressed=false и demand; отдельный существующий тест активации мышью не менялся.

Финальные шесть сценариев прошли с явным software GPU, отдельным процессом Chrome на каждую пару desktop/mobile: геометрия 35,6/26,8 с; подборка и архивы 34,1/43,7 с; immersive 34,8/36,5 с. Это локальные результаты; новый GitHub verify остаётся обязательным. Дополнительно неизменённые random/coarse/Nobel/writer/manual-drag/rapid-selection прошли в обычном Chrome 12/12, а визуальные эталоны immersive chrome и карточек писателей 2/2 без обновления PNG.
