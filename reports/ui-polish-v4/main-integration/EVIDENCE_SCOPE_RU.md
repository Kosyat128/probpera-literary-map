# Область доказательств при объединении с main

Дата: 2026-09-08. Сохранённая локальная приёмка [CURRENT_ACCEPTANCE_RU.md](../CURRENT_ACCEPTANCE_RU.md) от 2026-09-08T05:59:58.675Z относится к checkpoint `88a0830a` до объединения с новым `main` на `5d3f6fb6`.

Её source SHA-256: `9d483cf13525dbfe5d320e07eaf251692913d62bea7de214ba451aac0e560280`. 24 ограниченных кадра 1440/390, 13 областей / 12 границ и 70 уникальных выполненных E2E-сценариев с 8 отдельными пропусками остаются историческим подтверждением этого среза. Базовый прогон 65/5/8, addendum 1 2/3/1 и addendum 2 4/0/0 сохранены раздельно; числа означают passed/failed/skipped. Старые ошибки не переписываются в выдуманный полный зелёный прогон.

Эти результаты не являются доказательством качества нового объединённого дерева. Итог интеграции должен опираться на его фактический Git-срез, изменения, сборку и необходимые проверки затронутого поведения. Повторять неизменённые положительные сценарии без причины не требуется, но их историческая дата и область применения сохраняются.

## Состав коммита доказательств

[evidence-allowlist.json](evidence-allowlist.json) перечисляет ровно 153 отобранных файла относительно корня репозитория. Настоящие before/after, рабочие HTML-галереи, 37 требований, результаты проверок и benchmark-сводки сохраняются. Этот scope-файл и сам allowlist являются отдельными служебными документами интеграции.

В коммит не включаются trace ZIP, видео и большие artifact-деревья, гигантские fullPage-кадры, отвергнутые фоновые варианты, промежуточные working/recheck/dev-папки, временные capture/debug helpers и PNG чужих benchmark-сайтов. Они не удаляются с диска. Включённые небольшие журналы и диагностические объяснения сохраняют происхождение исправлений; они не переобозначаются как новая приёмка. Legacy brand-after-dev остаётся только зависимостью исторического сравнения фирменного знака в существующем отчёте.

Live writes, физические устройства, Safari/WebKit, Firefox, физический screen reader/принтер, полевые метрики и production HTTP404 сохраняют прежние not_verified-ограничения. Коммит/объединение в main сами по себе не являются разрешением на production-публикацию.
