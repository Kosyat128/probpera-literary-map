# Non-globe full E2E triage

Source: the active `final-complete-e2e.log` and saved `complete-e2e-artifacts/*/error-context.md`. No new browser was launched for this triage. Product source, background CSS and the canonical progress ledger were not changed. Targeted reruns remain pending the root agent's final build and completion of the current browser batch.

| Scenario | Confirmed failure | Narrow test adaptation | Preserved contract |
| --- | --- | --- | --- |
| `home-first-paint.spec.mjs`, static archive | Hardcoded `/stati/` received the preview server's base-path suggestion, not the archive. Saved snapshot explicitly points to `/probpera-literary-map/stati/`. Built archive HTML has static SEO/H1 and no module script. | Existing sitemap helper resolves the published base, then the test visits its archive root. | No module script, visible static SEO, exact archive H1. |
| `literary-news-production.spec.mjs`, language/dates | News/date/language/source assertions passed; final legacy `.news-editorial-context[open]` target was removed by approved R08. The same editorial copy is present in the saved snapshot. | After `#about`, assert hash, unique `#about`, and visible actual editorial-standard card inside `#reader-discussion`. | Lazy news request/chunk, timezone, reviewed mode, Russian and English content, visitor-relative dates, source URL, reachable editorial card. |
| `public-reader-journey.spec.ts`, desktop article journey | Hardcoded `/stati/` returns404 with the same explicit base-path suggestion. | Read the real unique archive href from the already-open homepage hero; navigate that URL. | HTTP status, H1, canonical, real archive article link, page/request/server diagnostics. |
| `public-reader-journey.spec.ts`, narrow home/archive | Same archive base mismatch. | Same actual public archive link helper. | HTTP status/H1,390px document/body overflow, keyboard focus, diagnostics. |
| `stage5b-art-direction.spec.mjs`, RU/EN roles | Collector throws on old `#featured-journal .journal-engagement h3`; editorial paragraph also uses former book-day location. R08 intentionally moved both into discussion. | Change only the two role targets to their current real discussion locations. | Existing Onest family, exact role size/leading ranges, metadata/actions, spacing and overflow checks. |
| Same Stage5B painted surfaces | The previous contract reads only CSS `backgroundImage`. Approved alpha artwork now uses real lazy `BrushBackdrop` images, and a base surface can correctly have CSS background-image none. | Visit each inspected surface to activate its existing observer, wait for actual image decode; accept non-none CSS background or a loaded, sized, visible pigment image. Empty wrappers do not pass. | A genuine painted surface must exist; all typography/geometry limits remain. |
| `stage5c-layout-community.spec.mjs`, homepage geometry | Test waits for old news-details summary and hidden editorial card, deliberately replaced by R08's visible discussion/right-column card. | Assert unique visible current card and preserved live news slot. Add discussion to the real DOM/visual section-order checks. | Existing card counts, row landmarks, book/news/fact geometry, mobile layout, community controls, overflow and section order. |

These changes adapt test setup and explicit superseded layout contracts. No claim that reruns passed is made here. No confirmed new non-globe product defect was found in these saved failures. Globe-related failures are handled separately by their owner.

Mobile entries148/149 repeated the initial archive/news failures during the same batch. Their `error-context.md` source excerpts show the current edited disk files, but the saved `trace.zip` records the actual old executed calls: `goto` URL `/stati/` (the new helper passes an absolute href) and an `expect` selector `.news-editorial-context` (removed from the adapted test). Thus the active batch retains its previously loaded test code; those entries are not reruns of the new adaptation. This was established by reading the trace archive, without starting a browser or making a new network probe.

## New reader defect found at mobile entry194

The expanded-text-spacing scenario reached the last page immediately after End, then lost the logical end during delayed reflow. Saved trace values at390px: before End page57/count59; immediately after End page58/count59; by the failed disabled assertion page59/count61. The screenshot shows page60of61. This is a confirmed end-intent/reflow race, distinct from the stale test setups above.

Scoped product correction: explicit End uses a semantic end target, and pagination continues to follow that end across delayed media/font/viewport measurement. A manual turn, outline navigation, mode exit or a new article clears this intent. Ordinary reading retains its existing first-visible-text anchor and storage contract. `ArticleReader.tsx` and `useArticleBookPagination.ts` are the only product files changed here.

The existing text-spacing test retains every assertion and additionally changes viewport height after End, requires the real page count to grow, confirms Next stays disabled, restores the original height, requires count to shrink and confirms the end again. This creates deterministic delayed reflow without changing authored text or remote content. Verification is pending the root's new build and targeted rerun; no new browser was started for this fix.

Root also assigned two bounded globe **setup** adaptations: `public-smoke:522` had already opened the wrong immersive search control (`aria-controls=atlas-search-panel`) while embedded; `responsive-reader-globe:375` tried to click the intentionally hidden input before the R05 search button. Both now open the actual embedded `data-atlas-action=toggle-search` control and require a visible input. Query retention, private-content exclusion, result metadata, flag geometry, containment and navigation instruction assertions remain. Globe product source is owned separately and was not changed here.
