# LiteraryMap SVG assets

This folder is reserved for the custom LiteraryMap world map assets.

Files planned here:
- `literary-world-map.svg` — the custom interactive world map
- `map-texture.png` — optional paper texture / overlay

The SVG is the primary map surface and will replace Leaflet/OpenStreetMap in the next step.
