import type { Country } from "./types";

/**
 * Central entry point for the LiteraryMap countries database.
 *
 * Country modules are stored separately in this folder.
 * They will be connected here as the main countries registry.
 */
export const countries: Country[] = [];
